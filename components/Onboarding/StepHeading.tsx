import React from "react";

const StepHeading = ({ headline }: { headline: string }) => {
  return <h1 className="mb-5 text-center font-playfair clamp">{headline}</h1>;
};

export default StepHeading;
